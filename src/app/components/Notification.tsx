import React from "react";

const Notification = () => {
  return (
    <div className="h-12 bg-red-500 text-white text-center px-4 text-sm md:text-base flex items-center justify-center cursor-pointer">
      Order over 600 TK for free delivery! Order Now!
    </div>
  );
};

export default Notification;
